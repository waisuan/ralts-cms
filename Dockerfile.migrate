FROM alpine:3.20

RUN apk add --no-cache \
    postgresql16-client \
    aws-cli \
    bash \
    coreutils

COPY scripts/backup-to-r2.sh /app/backup-to-r2.sh
RUN chmod +x /app/backup-to-r2.sh

WORKDIR /app
CMD ["./backup-to-r2.sh"]
