version: '3'

services:
  db:
    image: amazon/dynamodb-local
    restart: always
    container_name: dynamodb
    ports:
      - "8000:8000"
    volumes:
      - data:/home/dynamodblocal/data
    environment:
      - AWS_REGION=us-west-1
      - DYNAMODB_DATA_PATH=/home/dynamodblocal/data
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:8000 || exit 1"]
      interval: 1s
      timeout: 5s
      retries: 10

volumes:
  data: