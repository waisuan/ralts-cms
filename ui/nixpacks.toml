[variables]
NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci --no-audit"]

[phases.build]
cmds = ["CI=false npm run build"]

[start]
cmd = "npm start"
