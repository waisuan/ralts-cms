# UI service – Next.js frontend.
# In Railway: connect this service to the repo, set Config File Path to /ui/railway.toml.
# Root Directory: ui
# Variables: NEXT_PUBLIC_API_BASE_URL = Web service public URL

[build]
buildCommand = "npm ci && npm run build"
watchPatterns = ["ui/**"]

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/api/health"
